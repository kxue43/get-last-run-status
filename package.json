{
  "name": "get-last-run-status",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for getting the status of the last run of a workflow.",
  "scripts": {
    "build": "ncc build index.js --license licenses.txt"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}
